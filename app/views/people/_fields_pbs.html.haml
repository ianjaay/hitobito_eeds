-#  EEDS — Override de `_fields_pbs.html.haml` :
-#  on retire grade_of_school (Classe scolaire — système scolaire SN différent)
-#  ainsi que pronouns / title / salutation (Pronoms / Civilité / Formule de
-#  politesse — non pertinents dans le contexte EEDS).

= field_set_tag do
  = f.labeled_input_fields(:entry_date, :leaving_date)
